package org.firstinspires.ftc.teamcode.robotModel;

public class Launcher {

    // Return the required velocity of the launcher from the variables
    public double calculateVelocity(double distance, double airPressure) {
        // MARK: Constants
        // TODO: Setup
        double WING_AREA = 1;
        // TODO: Oliver: Calculate
        double COEFFICIENT_OF_LIFT = 1;
        return 0.0;
    }

            // Return the required motor speed (0-1) from the velocity
            public double calculateMotorSpeed(double velocity) {
        // Multiplier of motor speed outputted
        double GEAR_RAIO = 5;
                    return 0.0;
            }
}
